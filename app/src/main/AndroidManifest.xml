<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.belazy.chirppaypal">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SendReceiveActivity" />
        <activity android:name=".activity.RequestPaymentActivity" />
        <activity android:name=".activity.ChirpingActivity" />
        <activity android:name=".activity.DoneActivity" />
        <activity android:name=".activity.ConnectDemoActivity" />
        <activity android:name=".activity.PayPalWebViewActivity" />
        <activity android:name=".activity.SuccessFailureActivity"></activity>
    </application>

</manifest>